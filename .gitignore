.DS_Store
# The built binary, anchored to the root so it does not also swallow
# cmd/firstboot/, which is where the command actually lives.
/firstboot
dist/
# Generated by the release build from the source, not checked in.
completions/
